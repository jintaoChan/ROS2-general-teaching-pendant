# this file is used for general robot tool pendant

controller_manager:
  ros__parameters:
    database_buffer_size: 10000

    coordinate_system:
      # base_frame: 
      # tool_frame: 
      end_effector_frame: link5

    cartesian_limits:
      max_trans_vel: 0.1
      max_trans_acc: 0.02
      max_trans_dec: 0.05
      max_rot_vel: 1.0